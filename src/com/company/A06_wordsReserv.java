package com.company;

public class A06_wordsReserv {

    // PALABRAS RESERVADAS

    /**
     * java kyewords
     * https://docs.oracle.com/javase/tutorial/java/nutsandbolts/_keywords.html
     * https://www.w3schools.com/java/java_ref_keywords.asp
     *
     * abstract	continue	for	new	switch
     * assert***	default	goto*	package	synchronized
     * boolean	do	if	private	this
     * break	double	implements	protected	throw
     * byte	else	import	public	throws
     * case	enum****	instanceof	return	transient
     * catch	extends	int	short	try
     * char	final	interface	static	void
     * class	finally	long	strictfp**	volatile
     * const*	float	native	super	while
     *
     * En general las palabras reservadas aparecen en tono de azul
     * Recordar que las variables se escriben con la primer letra en MINÚSCULAS
     * y las clases se escriben con la primer letra en MAYÚSCULAS
     */


}
