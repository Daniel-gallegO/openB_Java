package com.company;

public class A05_operadores {

    public static void main(String[] args) {

        // OPERADORES

        // operadores aritméticos
        /**
         *  suma +
         *  resta -
         *  división /
         *  multiplicación *
         *  resto %
         *
          */

        int number1 = 1;
        int number2 = 2;

        int resultado1 = number1 + number2;
        int resultado2 = number2 - number1;
        int resultado3 = number1 * number2;
        int resultado4 = number2 / number1;


        // operadores lógicos, de relación o relacionales, de comparación, booleanos

        /**
         * mayor que >
         * mqyor o iguql que >=
         * menor que <
         * menor o igual que <=
         * igual que ==
         * distinto que !=
         * and (y) &&  (es verdadero si se cumplen todas las condiciones)
         * or (o) || (es verdadero si se cumple una o varias de las condiciones)
         * not (no, o negación) !
         *
         */


        // operadores de asignación

        /**
         * igual o asignación (diferente de igual que) =
         * sumar y asignar (se combina para realizar la operación y la asignación) +=
         * restar y asignar -=
         * multiplicar y asignar *=
         * dividir y asignar /=
         * resto y asignar %=
         */

        // operadores de variación

        /**
         * incremento (incrementa la variable numérica en 1) ++
         * decremento (decrementa la variable numérica en 1) --
         * ejemplo: numero1++;
         */

        // operadores de concatenación

        /**
         * unir o concatenar dos strings + (no es una suma aritmética sino que es una concatenación
         * ejemplo string1 + string2
         */

    }
}
